# ========================================
# REQUIRED: Database Configuration
# ========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Required for all database operations
DATABASE_URL="postgresql://postgres:password@localhost:5432/garritwulf"

# ========================================
# REQUIRED: MinIO Object Storage (S3-compatible)
# ========================================
# MinIO server endpoint (hostname or IP address)
# For local: "localhost", for Docker: "minio" or container name
MINIO_ENDPOINT="localhost"

# MinIO server port (default: 9000 for API, 9001 for console)
MINIO_PORT="9000"

# MinIO access credentials
# Generate secure credentials for production
MINIO_ACCESS_KEY="your-access-key"
MINIO_SECRET_KEY="your-secret-key"

# Use SSL for MinIO connection (true/false)
# Set to "true" in production with valid SSL certificates
MINIO_USE_SSL="false"

# Bucket name for storing uploaded files (images, documents, etc.)
# Must be lowercase, no spaces
MINIO_BUCKET_NAME="garritwulf"

# Optional: MinIO region (default: us-east-1)
# MINIO_REGION="us-east-1"

# ========================================
# REQUIRED: Clerk Authentication
# ========================================
# Get your keys from: https://dashboard.clerk.com/
# 
# Publishable key (starts with pk_test_ or pk_live_)
# Safe to expose in client-side code
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Secret key (starts with sk_test_ or sk_live_)
# NEVER expose in client-side code
CLERK_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# ========================================
# Application Settings
# ========================================
# Node environment (development, production, test)
NODE_ENV="development"

# Base URL for the application
# Used for generating absolute URLs (sitemaps, emails, etc.)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# ========================================
# OPTIONAL: Settings Encryption
# ========================================
# 64-character hex string for encrypting sensitive settings in database
# Generate with: openssl rand -hex 32
# SETTINGS_ENCRYPTION_KEY="your-64-char-hex-string"

# ========================================
# OPTIONAL: Stripe Payment Processing
# ========================================
# Only needed if implementing payment features
# Get keys from: https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# ========================================
# Setup Instructions
# ========================================
# 1. Copy this file: cp .env.example .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env.local to version control (already in .gitignore)
# 4. Required variables will be validated at startup
# 5. Missing required variables will prevent the application from starting